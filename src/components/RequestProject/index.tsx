import React from 'react'

type RequestProjectProps = Readonly<{}>

export default function RequestProject(props: RequestProjectProps): React.JSX.Element {
    return <div className='w-full bg-green-500 p-4'>RequestProject</div>
}
